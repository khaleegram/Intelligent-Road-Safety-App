{
  "name": "backend-firebase",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-roadsafe \"node --test tests/firestore.rules.test.mjs\"",
    "test:rules": "firebase emulators:exec --only firestore --project demo-roadsafe \"node --test tests/firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "async": "^3.2.6",
    "firebase": "^12.8.0",
    "firebase-tools": "^14.21.0"
  }
}
